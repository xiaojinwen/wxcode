pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Xposed API 仓库（多个备用源）
        maven { url 'https://api.xposed.info/maven2' }
        maven { url 'https://jcenter.bintray.com' }  // 备用
    }
}

rootProject.name = "wxcode"
include ':app'